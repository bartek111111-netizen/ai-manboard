/**
 * Shared constants and types used by the server and the web app.
 *
 * Phase 0 (skeleton) — engine types (InferenceEngine, ParamSchema, ...)
 * and config types are added in later phases (PLAN.md §22).
 */

/** Application name shown in the UI and in the API status endpoint. */
export const APP_NAME = 'AI Model Dashboard';

/** Application version (kept in sync with the root package.json). */
export const APP_VERSION = '0.1.0';
